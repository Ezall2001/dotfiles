#!/usr/bin/env nu

use ./actions.nu [copy]
use ./mod.nu [_main _menu]

def main [] { _main }
def 'main _menu' [address?:string] { _menu $address }
def 'main _action copy' [...selected] { copy $selected }
