#!/usr/bin/env nu

use /usr/bin/scripts/hyprland/hyp_file_select/mod.nu [_main _spf]

def main [--multi (-m), --dir (-d)] {
	_main --multi=$multi --dir=$dir
}

def 'main _spf' [dir:string] {
	_spf $dir
}
