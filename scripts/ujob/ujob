#!/usr/bin/env nu

use mod.nu [spawn await]

def main [] { }

def 'main spawn' [tag_name:string closure:closure] {
	spawn $tag_name $closure
}

def 'main await' [...tags --timeout (-t):duration] {
	await ...$tags --timeout=$timeout
}
