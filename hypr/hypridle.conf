listener {
	timeout = 300 # 5min
	on-timeout = hyp_idle 0
	on-resume = hyp_idle -w
}

listener {
	timeout = 600 # 10min
	on-timeout = hyp_idle 1
	on-resume = hyp_idle -w
}

listener {
	timeout = 900 # 15min
	on-timeout = hyp_idle 2
	on-resume = hyp_idle -w
}

