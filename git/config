[include]
	path = ./advice.conf
	path = ./alias.conf

[user]
	email = armen.bakir.e@gmail.com
	name = Ezall2001

[alias]
	graph = log --oneline --graph --decorate -n10
	dswitch = switch --detach

[pull]
	ff = only

[web]
	browser = zen

[browser "zen"]
	cmd = /sbin/zen-browser

[core]
	editor = nvim
	pager = less
	sshCommand = ssh -F /home/armen/.config/ssh/config
	excludesFile = /home/armen/.config/git/.gitignore
	autocrlf = input

[diff]
	external = difft --context 3 --tab-width 2 --display side-by-side --background dark --syntax-highlight on --color always
